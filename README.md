# Mini Amazon (Java prototype)

This is a minimal Java prototype demonstrating product listing, product details, and a simple in-memory cart.

Run with Maven:

```powershell
mvn compile exec:java
```

Then open http://localhost:4567/
# 26janjavacodeci
